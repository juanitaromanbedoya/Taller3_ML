# Taller-3_pythonML

# Pasos para ejecutar el sisitema

# 1. Craer entorno virtual 
# 2. Instalar Libresias: En los archivos 2 y 3 (# pip install scikit-learn -  #pip install joblib)
# (# pip install streamlit -  # pip install plotly)
# 3. Ejecuatar el proyecto en local: Rectificas que estes en la carpeta raiz que en este caso es (RandomForest) y ejecutas streamlit run el nombre del archivo    
# 4. Desployment 
